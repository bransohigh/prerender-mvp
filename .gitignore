node_modules
dist
.env
coverage
playwright-report
.DS_Store
# Ephemeral CI-only TLS materials generated by docker/gateway/generate-ci-cert.sh
# — never committed, 1-day validity, regenerated fresh per CI run.
ci-certs/
